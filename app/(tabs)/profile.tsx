// This screen is just a placeholder - the actual profile switcher opens a bottom sheet
// This file exists to add the profile tab to the navigation
import React from 'react';
import { View } from 'react-native';

export default function ProfileTabScreen() {
  // This screen should never be shown - the tab button will open the bottom sheet instead
  return <View />;
}

